<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TestMovies.Pages.SettingsContentPage"
             Style="{x:StaticResource pageStyle}"
             Title="Settings">
    <StackLayout>
        <Grid Margin="20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70*"/>
                <ColumnDefinition Width="30*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Label Grid.Column="0" Grid.Row="0" Text="Default Language" TextColor="White" FontAttributes="Bold" FontSize="Default" VerticalOptions="Center"/>
            <Switch Grid.Column="1" Grid.Row="0" IsToggled="True" ThumbColor="Blue" OnColor="White" VerticalOptions="Center" HorizontalOptions="End"/> 
        </Grid>

        
    </StackLayout>
</ContentPage>
